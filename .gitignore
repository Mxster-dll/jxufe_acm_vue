node_modules
dist
.deploy/
dist-ssr
*.local
.DS_Store
public/share/
.vscode/*
!.vscode/extensions.json

# 生成物：由 prebuild 的 scripts/gen_*.mjs 每次都重新生成，不入库。
# 入库只会带来两种噪声：纯粹的 generated_at 时间戳 diff、以及「生成物比生成器活得久」
# 造成的静默陈旧数据（例：把 wall_rules.json 的 excellentScoreThreshold 改成 0 后，
# 页面仍在读上一次落盘的名单）。部署不受影响 —— deploy.bat 用 tar 打包本地工作树
# （不是 git 跟踪文件），服务器端 npm run build 的 prebuild 会重新生成它们。
public/data/group_wall.json
public/data/group_wall.manifest.json
public/data/excellent_members.json
public/data/event_badges.json
# hero_wall.manifest.json 同理，2026-09-24 从入库转出：它是 gen_hero_wall.mjs 扫
# public/images/excellent_member/ 出来的清单，内容只有文件名 + generated_at，
# 而应用里**没有消费者** —— 首页那面墙由 HomeView.vue:253 显式传
# manifest-url="/data/group_wall.manifest.json"，组件里那个 hero_wall 默认值从不生效。
# 注意它与 hero_wall.json 不是一回事：后者是**会长手写的留言源**，gen_group_wall.mjs
# 会读它（按姓名并入成员墙），必须继续入库。
public/data/hero_wall.manifest.json
*.log
